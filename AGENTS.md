# 작업 원칙

- 해결책을 설계하기 전에 기존 제품, 공식 문서, 이 코드베이스의 유사 구현을 먼저 조사하고 검증된 패턴과 관례를 따르세요.
- 쓰이지 않는 내부 경로는 호환 레이어나 폴백을 덧붙이지 말고 호출처를 확인한 뒤 삭제하세요. 공개 URL, API, Webhook 계약, 저장 데이터 스키마의 하위 호환은 명시적인 마이그레이션 작업이 아니면 깨지 마세요.
- 현재 요구사항을 완전히 충족하는 가장 단순한 구현을 선택하고, 추측에 근거한 추상화, 설정, 간접 계층을 만들지 마세요.
- 엔드투엔드로 동작하는 최소 단위부터 만들고 검증한 뒤 기능을 하나씩 더하세요. 동작하는 결과물을 미완성 복잡도와 맞바꾸지 마세요.
- 컴포넌트와 로직은 책임이 분명한 모듈로 나누되, 한 번만 쓰는 작은 코드까지 성급하게 추상화하지 마세요.
- 검증되고 유지보수되는 라이브러리가 복잡도를 낮추거나 안정성을 높이면 사용하고, 흔한 기능을 명확한 이유 없이 재구현하지 마세요.
- 직접 구현하거나 패키지를 추가하기 전에 설치된 의존성, 공식 문서, 타입, 기존 유틸리티를 확인하세요. 확인 없이 기능이 없다고 단정하지 마세요.
- 아키텍처는 장기적으로 유지할 구조를 선택하세요. 임시방편이 필요하면 숨기지 말고 범위, 제거 조건, 위험을 먼저 밝히세요.

# 프로젝트 예외

- 기존 작업과 미추적 파일을 보존하세요. 요청 범위 밖의 변경을 되돌리거나 삭제하지 마세요.
- 거짓 성공 상태나 동작하지 않는 UI를 만들지 마세요. 실제 API를 연결하거나 실제 계약과 호환되는 목(mock)을 사용하고, 아직 막힌 기능은 상태와 다음 행동을 솔직히 표시하세요.
- 비밀값과 개인정보를 코드, 로그, 문서에 넣지 말고, 민감한 데이터는 최소 수집하며 삭제 경로를 함께 유지하세요.
- 완료 전 변경 범위에 맞게 `npm run typecheck`, `npm run test:unit`, 관련 Playwright 테스트를 실행하고, 실행하지 못한 검증은 이유와 함께 명시하세요.

# 공통 응답 문체

- 모든 본문에서 em dash 문자 `U+2014`를 사용하지 않습니다. 문맥에 맞게 쉼표, 콜론, 괄호, 마침표로 바꿉니다.
- 키워드나 항목을 나열할 때 middle dot 문자 `U+00B7`를 사용하지 않습니다. 쉼표, `와` 또는 `과`, `및`, 슬래시 중 문맥에 맞는 구분자를 사용합니다.
- 해요체와 습니다체를 요청된 `N:N` 비율에 가깝게 자연스럽게 섞습니다. 비율이 따로 없고 기업용 블로그라면 기본값은 `5:5`입니다.
- 사용자가 문체나 비율을 더 구체적으로 지정하면 그 요청을 우선합니다.
