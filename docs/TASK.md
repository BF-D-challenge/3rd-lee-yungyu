# 맛핀 실행 TASK

> 기준일: 2026-08-10
>
> 기준 명세: [맛핀 제품 요구사항](PRD.md)

각 완료 표시는 코드, 마이그레이션 또는 실행 결과로 확인한 항목만 뜻합니다. 운영 배포 전 항목은 별도로 구분해요.

## T0. 입력 계약과 대화 안내

- [x] 공개 릴스 공유 처리
- [x] 공개 피드 게시물 공유 처리
- [x] 공개 캐러셀의 확인 가능한 미디어 처리
- [x] DM 본문의 공개 릴스 또는 개별 게시물 링크 처리
- [x] DM에 직접 보낸 이미지와 동영상 제외
- [x] 일반 글, 프로필 주소, 외부 주소와 지원하지 않는 첨부파일 제외
- [x] 제외 입력을 DB와 AI 분석에 전달하지 않고 유형별 사용 방법 답장
- [x] 접수, 중복, 저장 성공, 장소 없음과 처리 불가 상태별 DM 문구 제공

## T1. 분석과 저장 파이프라인

- [x] Meta Webhook 검증 토큰과 요청 서명 확인
- [x] 중복 Meta 메시지 접수 방지
- [x] Webhook에서 DB 접수와 암호화 outbox 등록만 끝낸 뒤 빠르게 응답하고 Meta 발송은 Worker로 분리
- [x] 접수 답장이 terminal이 된 뒤에만 분석 큐를 정확히 한 번 열고, final 답장이 먼저 발송되지 않도록 순서 보장
- [x] `leased`와 `sending`을 분리해 전송 시작 전 중단만 복구하고, 전송 시작 뒤 결과 불명은 `uncertain`으로 종결
- [x] 큐 기반 장소 분석과 제한된 재시도
- [x] 분석 claim token으로 stale Worker의 저장, 완료와 재시도를 차단하고 같은 token 완료 재호출은 읽기 전용으로 멱등 처리
- [x] 두 번째 분석 실패와 poison 입력을 generic 실패 안내 또는 원문 없는 격리 상태로 종결해 큐 head 고착 방지
- [x] 장소 후보 최대 3곳 추출 및 실제 지도 장소 대조
- [x] 한 게시물에서 찾은 모든 장소 저장
- [x] 사용자별 저장 장소와 분석 사용량 기록
- [x] 처리 실패 건의 안전한 재처리 경로

## T2. 영구 분석 캐시

- [x] 미디어 식별자 기준 분석 캐시 테이블 추가
- [x] 완료된 `resolved`와 `insufficient` 결과를 만료일 없이 재사용
- [x] 동시에 들어온 같은 미디어의 중복 분석을 임대 상태로 방지
- [x] UUID claim token과 300초 lease로 만료 뒤 새 소유자를 stale 완료와 release에서 보호
- [x] 캐시 적중 처리 결과의 AI 요청과 토큰 사용량을 0으로 기록
- [x] 실패 재처리 시에만 해당 미디어 캐시 무효화
- [x] 캐시 처리 상태용 미사용 인덱스 제거

## T3. 개인 보관함과 짧은 주소

- [x] 사용자별 암호화 식별자와 해시 접근 토큰
- [x] 짧은 `/s/{code}` 주소로 개인 보관함 연결
- [x] 가까운 역별 장소 묶음과 최신 저장 순
- [x] 게시물별 장소 목록과 원본 게시물 연결
- [x] 저장 장소 개별 삭제
- [x] 사용자 전체 데이터 삭제 경로
- [x] 저장 완료와 재전송 DM에서 짧은 보관함 주소 사용

## T4. 운영 CRM 어드민

- [x] `/matpin/admin` 데스크톱 우선 반응형 화면
- [x] Supabase Google 로그인과 `MATPIN_ADMIN_EMAILS` 허용목록
- [x] 허용목록 미설정 시 전체 접근 거부
- [x] 운영 지표 기간 필터와 대화 상태 필터
- [x] Meta API 기반 최근 대화와 메시지 최대 20건 실시간 조회
- [x] 프로필 조회 실패 시 이름 없는 대화로 계속 표시
- [x] 일부 대화 상세 조회 실패를 알리는 부분 성공 상태
- [x] 맛핀 DB의 처리 이력, 저장 장소와 사용량 병합
- [x] 실패 메시지만 한 건 재처리
- [x] 저장 장소가 있는 사용자만 보관함 주소 재전송
- [x] 최근 사용자 수신 후 24시간 미만일 때만 자유 문구 DM 활성화
- [x] 관리자 자유 문구가 비어 있지 않고 UTF-8 기준 1,000바이트 이하인지 확인하고 서버에서 수신자 재확인
- [x] 수신자와 최종 문구 확인 뒤 한 명씩 발송
- [x] 멱등성 키로 중복 클릭 방지
- [x] 원문 없는 관리자 행동 감사 로그
- [x] 관리자 API의 `private, no-store` 응답과 민감 필드 제외

로컬 구현과 아래 운영 DB 마이그레이션은 확인했습니다. 다음 세 결함은 수정하고 회귀 테스트를 통과했지만, 현재 운영 배포와 실제 관리자 로그인은 아직 검증하지 않았어요.

- [x] Meta 대화 조회가 정해진 전체 요청 시간 예산 안에서 끝나고, 초과 시 부분 성공으로 안전하게 반환되는지 보장
- [x] 발송 결과가 불확실한 상태를 `uncertain`으로 종결하고, 동시 요청과 자동 재전송의 중복 발송 위험 제거
- [x] 관리자 자유 문구뿐 아니라 자동 답장에도 UTF-8 기준 1,000바이트 제한 적용

## T5. 데이터베이스

- [x] 대화 이력용 `acknowledged_at`과 재접수 계약 적용
- [x] 미디어 영구 분석 캐시 적용
- [x] 짧은 보관함 주소 적용
- [x] 분석 API 요청과 토큰 사용량 기록 적용
- [x] `matpin_admin_actions` 감사 테이블 적용
- [x] 관리자 메시지 조회용 인덱스 적용
- [x] 운영 Supabase 관리자 CRM 마이그레이션 7개 반영 및 현재 DB에서 존재와 실행 권한 확인
- [x] Durable outbox 마이그레이션을 새 PostgreSQL에 처음부터 적용하고 ordering, claim fencing, TTL, reprocess와 ACL 경합 재현 통과
- [ ] 운영 Supabase에 `20260809174423_add_matpin_delivery_attempt_claims.sql` 적용

## T6. 로컬 검증

- [x] 관리자 인증의 401, 403과 미설정 거부 단위 테스트
- [x] 대화 목록, 부분 성공, 최근 20건 제한 단위 테스트
- [x] DM 길이, 24시간 제한, 수신자 재검증과 멱등성 단위 테스트
- [x] 실패 재처리와 보관함 재전송 단위 테스트
- [x] 관리자 접근, 미리보기 확인과 모바일 전환 Playwright 테스트
- [x] `npm run typecheck`
- [x] `npm run test:unit` 전체 68개 파일, 501개 테스트
- [x] `npm run test:e2e` 전체 123개 테스트
- [x] `npm run lint`
- [x] `npm run build`

## T7. 운영 배포 전 필수 작업

- [ ] 실제 관리자 Google 이메일을 확정하고 로컬 및 배포 환경의 `MATPIN_ADMIN_EMAILS`에 등록
- [x] T4의 출시 전 코드 결함 3개를 수정하고 관련 단위 테스트와 Playwright 테스트 재실행
- [x] Durable outbox, 255초 Worker deadline, 300초 cache lease, 600초 PGMQ visibility와 POST poller kick의 로컬 구현 및 fresh PostgreSQL 검증
- [ ] 관리자 감사 로그의 보존 기간과 계정 삭제 시 처리 정책 확정 및 구현 범위 결정
- [ ] 맛핀 변경 파일만 커밋하고 다른 제품 및 사용자의 미완료 변경이 섞이지 않았는지 확인
- [ ] 현재 브랜치를 원격에 올리고 검토 가능한 변경 범위를 확정
- [ ] 유지보수 시간대를 확정하고 `maintenance` 배포, poller 중지, 최소 360초 대기, `received=0`, `processing=0`, PGMQ 0건과 관리자 pending 0건 확인
- [ ] 위 drain 상태에서 outbox 마이그레이션과 service role 전용 ACL을 적용하고 신규 앱을 운영 별칭 100%에 연결
- [ ] Supabase Vault와 Cron에 30초 독립 poller를 등록하고 HTTP 202뿐 아니라 Vercel 로그와 DB 및 큐 forward progress 확인
- [ ] Vercel에 배포한 뒤 `matpin-kr.vercel.app` 운영 별칭이 새 Ready 배포를 가리키는지 확인
- [x] `npm run matpin:launch:check -- https://matpin-kr.vercel.app`을 실행해 현재 운영 CRM 화면과 미로그인 차단이 각각 HTTP 404로 실패함을 확인
- [ ] 신규 배포 뒤 같은 출시 점검의 모든 항목 통과
- [ ] 허용된 Google 계정으로 `/matpin/admin` 실제 로그인
- [ ] Meta에서 실제 대화 한 건과 최근 메시지 최대 20건 조회
- [ ] 소유한 테스트 계정 한 곳에만 자유 문구 DM 발송
- [ ] 발송 감사 로그와 DM 원문 미저장을 DB에서 대조
- [ ] 실제 공개 릴스, 피드 게시물, 캐러셀과 링크를 각각 한 번씩 왕복 테스트
- [ ] 같은 게시물을 다시 보내 캐시 적중과 추가 AI 요청 0건 확인
- [ ] 실제 한 건에서 receipt terminal 전 분석 0건, terminal 뒤 분석 1건, final 1건과 outbox terminal 암호문 `NULL` 확인
- [x] 개발 도구의 `brace-expansion`과 `js-yaml` 취약 버전을 갱신하고 깨끗한 설치 및 전체 검증 재실행 (운영과 개발 의존성 감사 0건)

## T8. 공개 출시 게이트

- [x] `instagram_business_basic`과 `instagram_business_manage_messages`의 현재 상태가 테스트 준비 완료이며 앱 검수 미제출임을 재확인
- [ ] Meta App Review에 필요한 메시지 권한과 증거를 추가하고 승인 완료
- [ ] 앱 역할이 없는 일반 Instagram 계정으로 공유, 자동 답장, 저장과 보관함 열기 전 과정 확인
- [ ] 메시지 요청함의 이벤트 수신과 자동 처리 범위를 실제 계정에서 확인
- [x] Meta 공식 계약상 별도 수락 API가 없고 첫 API 답장이 요청함 대화를 General로 이동시키는 동작 확인
- [ ] 앱 역할이 없는 비팔로워 계정으로 요청함 Webhook 수신과 첫 답장 뒤 General 이동 확인
- [ ] 처리 대기, 실패, 미답장과 API 사용량을 관리자 화면 및 DB 수치로 대조
- [ ] 개인정보 응답 DTO에 scoped ID, access token, 암호문과 임시 미디어 주소가 없는지 운영 응답에서 재확인
- [x] 운영 Meta 이벤트와 Clarity 수신 상태를 읽기 전용으로 재확인
- [ ] GA4에서 맛핀 랜딩 이벤트를 제품별로 분리해 운영 수신 확인

T7과 T8이 끝나기 전에는 로컬 구현 완료를 운영 출시 완료로 표현하지 않습니다. 내부 또는 역할 계정 테스트 성공과 일반 사용자 공개 준비도도 구분해요. Meta 고급 액세스 상태와 앱 역할이 없는 일반 계정 왕복을 모두 확인하기 전까지 공개 출시는 `HOLD`입니다.

## T9. 별도 로컬 산출물과 제품 범위

- [x] 발표 C안의 개인정보 가림과 브라우저 재QA 완료
- [x] Moonlight 카드 상호작용을 로컬 HTTP 환경에서 실제 포인터 동작으로 재QA
- [ ] Onebite와 Today 확장 시안을 다시 시작하기 전에 현재 맛핀 정본을 바꾸는 별도 제품 결정 확정

발표 C안은 `outputs/presentation-candidates/` 아래의 별도 로컬 산출물이며 현재 Git에서 무시됩니다. 9장과 정확히 240초, 데스크톱 및 모바일 넘침 없음, 개인정보 가림과 최종 광고 수치를 브라우저에서 확인했어요. Moonlight도 맛핀 출시 범위와 분리해 실제 포인터 종료 상태까지 검증했습니다. 과거 Onebite와 Today 확장 시안은 현재 맛핀 정본과 충돌하므로, 제품 결정을 새로 확정하기 전에는 재개하지 않습니다.
